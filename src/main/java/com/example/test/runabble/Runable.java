package com.example.test.runabble;

public class Runable {
    public static void runable(Integer threads){

//    int finalNumbers = numbers;
//    int[] numberArray = Util.generate(finalNumbers);
//
//    int[] totArray = new int[threads];
//    Thread[] threadArray = new Thread[threads];
//
//        for (int i = 0; i < threads; i++) {
//        final int index = i; // Capture the index variable for the lambda expression
//        threadArray[i] = new Thread(() -> {
//            totArray[index] = Util.countPrimes(numberArray, threads);
//        });
//    }
//
//    long t1 = System.currentTimeMillis();
//
//        for (Thread t : threadArray) {
//        t.start();
//    }
//
//        for (Thread t : threadArray) {
//        t.join();
//    }
//
//    // Calculate the total value for all 'tot' values
//    int total = 0;
//        for (int tot : totArray) {
//        total += tot;
//    }
//
//    long timeDelta = System.currentTimeMillis() - t1;
//        listData.add("time = " + timeDelta + " miliseconds.");
//
//

}
}
